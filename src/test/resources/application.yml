spring:
#  datasource:
#    url: jdbc:h2:mem:test // h2 데이터베이스가 메모리 모드로 돌도록 설정해 주는 url
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

# 스프링 부트에서는 이 설정들이 다 없어서 자동으로 메모리 모드로 동작함.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

